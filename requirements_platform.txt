# Axalon Platform — Additional Dependencies
# Install AFTER the existing requirements.txt:
#   pip install -r solar_thermal_detection/requirements.txt
#   pip install -r requirements_platform.txt
#
# System dependencies for PDF generation (WeasyPrint):
#   sudo apt install -y libpango-1.0-0 libpangoft2-1.0-0 libpangocairo-1.0-0

# ── REST API ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # FastAPI file upload support

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.34.0

# ── OCR — Panel number recognition ───────────────────────────────────────────
easyocr>=1.7.1
# Alternative (lighter, needs system Tesseract):
# pytesseract>=0.3.10

# ── Geospatial ────────────────────────────────────────────────────────────────
rasterio>=1.3.9                  # GeoTIFF orthomosaic processing
geopandas>=0.14.3                # Spatial dataframes + GeoJSON export
pyproj>=3.6.1                    # Coordinate reference system transforms
piexif>=1.1.3                    # GPS EXIF metadata parsing
# Note: Pillow is already in requirements.txt (>=10.2.0)

# ── Report generation ─────────────────────────────────────────────────────────
jinja2>=3.1.4                    # HTML template engine for PDF
weasyprint>=61.2                 # HTML → PDF renderer
openpyxl>=3.1.2                  # Excel .xlsx export

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.29               # ORM (SQLite local, PostgreSQL production)
alembic>=1.13.1                  # Database migrations

# ── API utilities ─────────────────────────────────────────────────────────────
pydantic>=2.7.0                  # Request/response validation (FastAPI uses this)
python-dotenv>=1.0.1             # .env file support for secrets

# ── Already in requirements.txt but listed for clarity ───────────────────────
# tqdm>=4.66.0                   # Progress bars (already present)
# numpy>=1.26.0                  # (already present)
# opencv-python>=4.9.0           # (already present)
# Pillow>=10.2.0                 # (already present)
